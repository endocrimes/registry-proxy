{
  "devDependencies": {
    "@fermyon/knitwit": "https://github.com/fermyon/knitwit",
    "webpack": "^5.93.0",
    "webpack-cli": "^5.1.4"
  },
  "scripts": {
    "dev": "wrangler dev src/index.ts",
    "deploy": "wrangler deploy --minify src/index.ts",
    "lint": "eslint src/**/*.ts",
    "build": "npx webpack --mode=production && npx mkdirp target && npx j2w -i dist.js -d combined-wit -n combined -o target/app.wasm"
  },
  "dependencies": {
    "@fermyon/spin-sdk": "^2.0.0-alpha.3",
    "hono": "^3.12.8",
    "ts-loader": "^9.5.1"
  }
}
